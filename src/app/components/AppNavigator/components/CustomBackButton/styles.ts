import {StyleSheet} from 'react-native';
import { monza } from 'src/constants/colors';

export default StyleSheet.create({
  container: {
    flexDirection: "row"
  },
  image: {
    height: 20,
    width: 20,
    resizeMode: "cover",
  },
});
